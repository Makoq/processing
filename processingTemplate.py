import sys
import os #导入模块
# input,output分别为输入、输出数据集文件夹目录，此外是处理方法参数，如这里的CityName（可选项）
def execute(input,output,cityName):
    # 定义处理中间件，可调用外部script,exe,lib,api等
    # do something
if __name__ == "__main__":
    if execute(sys.argv[1],sys.argv[2],sys.argv[3]) is True:
        print("ok")
      
